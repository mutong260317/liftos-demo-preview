# LiftOS V0.3.1 Stabilization Audit

Baseline: `main@e55df0df2cfdcca621be47126519d31c9ad83e09` (V0.3.0)  
Branch: `stabilize/v0.3.1-audit`  
APP_VERSION: **0.3.1**  
CURRENT_SCHEMA_VERSION: **5** (unchanged)

## Totals (cumulative after Review R1 + R2)

| Severity | Found | Fixed |
|----------|------:|------:|
| P0 | 7 | 7 |
| P1 | 9 | 9 |
| P2 | 5 | 2 |

---

## P0

### P0-A — `countSeedPRs()` guessed PRs
- Removed; PR count only from real `h.prs`. **Fixed**

### P0-B — `?demo=1` polluted production history
- Demo overlay in `sessionStorage`; production keys untouched. **Fixed**

### P0-C — Import proceeded when backup failed
- Abort with `BACKUP_FAILED`. **Fixed**

### P0-D — `replaceExercise` orphan superset
- Inherit/unlink + `normalizeSupersetGroups`. **Fixed**

### P0-E — Duration progression suggested kg
- Duration → hold / no weight suggestion. **Fixed**

### P0-F — Corrupted localStorage silent empty
- `detectCorruption` / `preserveCorruptValue`; write preserves raw first; migration blocked on corrupt. **Fixed (R1)**
- **R2:** startup `preserveAllCorruptKeys()`; toast only after recovery copy exists; `exportPayload()` **throws** `CORRUPT_EXPORT*` instead of silent empty export; tests for history/plans/prefs. **Fixed**

### P0-G — Stopwatch wrong-exercise writes
- Bound to `{sessionId, exerciseUid, exerciseId, setId}`; cleanup on nav/skip/next/replace/end/abandon/finish/delete. **Fixed (R2)**

---

## P1

### P1-A Fresh neutral prefs
### P1-B Unused `importSchema` / public `__failAt`
### P1-C Version 0.3.1 + SW cache
### P1-D Assisted UI format (formatter)
### P1-E Superset orphan on plan remove + session skip
### P1-F Undo/detail use `formatLoad`
### P1-G Stopwatch lifecycle (end/abandon)
### P1-H SW per-asset cache; core shell required
### P1-I Preview smoke 0.3.1 label
### P1-J **R2:** HTTP + SW + offline Playwright smoke (`qa-v031-sw-http.js`)
### P1-K **R2:** `controllerchange` no auto-reload during active workout
### P1-L **R2:** DOM tests for assisted Undo + plank history detail

---

## P2
- Root duplicate `app.js` — deferred  
- Review markdown in `scripts/` — keep until close  
- Wake-lock test hook cleanup — deferred  
- Deeper a11y — deferred  
- `Gym.__setWakeLockCounters` — deferred  

## Migration
Schema **v5 unchanged**.

## Test counts (latest)
- qa-v031-stabilization: see CI log (35 + R2 additions)
- qa-v02 44/44, review 32/32, v021 37/37, v03 60/60
- qa-v031-sw-http: SW/offline smoke

